# Карта архитектуры AM Graph Viewer

C++17: CLI `lvm_reader` и нативный Win32 GUI используют общее ядро чтения
LVM/TXT/CSV и анализа сигналов. Ниже имена модулей означают пары `.cpp/.hpp`,
если расширение не указано. Начинай с заголовка нужного модуля и поиска символа.

## Поток данных и основные связи

`main.cpp` (CLI) или `gui_loading` → `lvm_parser` + `data_io` → `lvm::Dataset`.
Dataset содержит время, исходное время, каналы и метаданные. Парсер также
распознаёт сохранённые спектры; нормализация времени и удаление дубликатов
временных колонок учитывают тип данных.

GUI хранит документ и представление в общем `gui::App` (`gui_state`, экземпляр `g`).
`gui_processing` применяет формулы/фильтры и управляет их кэшами;
`gui_render_data` даёт значения и диапазоны для `gui_render`.
`gui_spectrum` готовит выбранный участок и каналы → `spectrum_worker` →
`analysis` → `fft`; результат принимается с учётом поколения запроса.
`gui_export_metadata` использует данные/обработку/спектр и `data_io` для записи.

## Где менять поведение

| Область | Основные файлы и ответственность |
|---|---|
| Запуск и команды GUI | `gui_main`: вход, цикл сообщений, маршрутизатор `WndProc`; `gui_window`: создание/размер/закрытие окна; `gui_commands`: меню, кнопки, ползунки; `gui_input`: мышь, клавиши, выделение |
| Размещение и навигация | `gui_layout`, `gui_side_panel`: расположение и состояние элементов; `gui_navigation`: масштаб/панорама; `gui_playback`: воспроизведение/таймеры |
| Отрисовка | `gui_render`: графики; `gui_render_data`: значения и границы; `minmax_index.hpp`: точные экстремумы для больших данных; `gui_time_axis`: склеенная шкала; `gap_details`: сведения о разрывах |
| LVM/TXT/CSV | `lvm_parser`: Dataset, парсинг, нормализация времени, индекс сканирования и чтение диапазона; `data_io`: CSV/TSV, Unicode-пути, безопасная запись |
| Загрузка | `gui_loading`: сканирование/фоновая загрузка, отмена, применение результата; `gui_loading_drop`: окно прогресса и drag-and-drop |
| FFT | `analysis`: подготовка выборки, спектр/пики, импорт готового спектра; `fft`: математическое преобразование; `sampling.hpp`: шаг и критерий разрыва; `spectrum_worker`: фоновые задания; `gui_spectrum`: выделение, кэш, применение результата |
| Формулы и фильтры | `formula_engine`: разбор/вычисление формул; `filter_engine`: цифровые фильтры; `gui_processing`: связь обработки с каналами и кэшами |
| Экспорт | `gui_export_metadata`: таблицы, спектры, PNG и метаданные повторного открытия; `export_helpers`: диапазоны, имена, подписи; `data_io`: запись с сохранением прежнего файла при ошибке; CLI-экспорт — `main.cpp` |
| Настройки и диалоги | `gui_settings`: INI, недавние файлы, Light Mode; `gui_settings_hotkeys`: окно настроек, сочетания клавиш, меню; `gui_dialogs`: ввод значений, диапазона, экспорт; `gui_welcome`: стартовый экран |
| Состояние и оформление | `gui_state`: App и общие типы; `gui_state_history`: измерения, снимки, Undo/Redo; `gui_theme`: темы/ресурсы; `gui_text`: переводы; `gui_status`: статус; `gui_ids.hpp`: ID элементов; `gui_platform.hpp`: общие зависимости |
| CLI и поставка | `main.cpp`: аргументы, чтение, статистика, выборка, FFT, CSV; `version.hpp`: версия CLI; `build_cli.ps1`, `build_gui.ps1`, `Makefile`: сборка; `package_release.ps1`: упаковка; `Start GUI.bat`/`run.bat`: запуск GUI/CLI |

## Ограничения, которые важно сохранять

- FFT использует доступные отсчёты всего выбранного участка, сжимая пропуски.
  Явный лимит отсчётов может сократить выборку; неточные метки времени требуют
  интерполяции. `sampling.hpp` используется также экранной склейкой.
- Экранная склейка не изменяет исходное время Dataset и экспорта. Готовый
  спектр открывается без повторного FFT и временной обработки амплитуд.
- В Light Mode FFT выполняется в фоне. Скрытие/возврат каналов текущего кэша
  не требует пересчёта; новый канал, участок или обработка — требуют.
- Реализации GUI компилируются отдельно, `.cpp` не включаются друг в друга.
  Приложение и GUI-тесты используют те же объектные файлы. Заголовки
  самодостаточны, определения глобальных объектов находятся в одном `.cpp`.
  Новые оконные сообщения проходят через `gui_main` в профильный обработчик.
- Undo/Redo хранит изменения настроек и измерений; изменения обработки должны
  согласованно обновлять кэши и историю. Изменяемое состояние пока общее в App.

## Проверки и служебные файлы

- `tests/run_tests.cpp`: чтение, формулы, FFT/пропуски, диапазоны, min/max,
  импорт спектра, сохранность записи. Запуск из корня: `make test`.
- `tests/gui_regression.cpp`: экспорт/повторное открытие, Light Mode, фоновые
  FFT, склейка, история и сообщения окна. Windows: добавь `-Test` к команде
  сборки GUI ниже или используй `make test-gui`. Проверяет логику, не вид окон.
- Полная сборка обоих приложений: команды ниже либо `make all gui` на Windows.
  Скрипты рассчитаны на передачу аргументов через Windows PowerShell;
  запускай их через `powershell.exe`, в том числе из PowerShell 7.

  ```powershell
  powershell.exe -NoProfile -ExecutionPolicy Bypass -File ./build_cli.ps1
  powershell.exe -NoProfile -ExecutionPolicy Bypass -File ./build_gui.ps1
  ```

- CI: `.github/workflows/build.yml` — ядро на Linux, оба набора и обе программы
  на Windows при push/pull request.
- `lvm_files_for_tests/test.lvm` — постоянный тестовый пример. `tests/_tmp_*`,
  тестовые EXE, `.build/` и объектные файлы — воспроизводимые результаты.
  Прямой запуск EXE не пересобирает тест; GUI-тест оставляет временный экспорт.
- `docs/archive/` — необязательные локальные отчёты, исключённые из Git;
  их отсутствие нормально. История опубликованных изменений — `CHANGELOG.md`.
  `docs/assets/` — изображения README; `AMGV.ini` — локальные настройки.
